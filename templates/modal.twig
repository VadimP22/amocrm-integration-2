
<div class="{{bg}} m-auto rounded-lg p-4 w-1/4 min-w-fit">
    <div id={{prefix ~ '-modal-content'}} class="{{bg}}"></div>
    <div><p id={{prefix ~ '-modal-close-button'}} class="text-right mt-4 font-semibold text-red-500 cursor-pointer">Закрыть</p></div>  
</div>
